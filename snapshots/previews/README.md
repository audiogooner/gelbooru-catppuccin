# Selector previews

Element screenshots captured by **Alt+Shift+S**, used for hover previews in the SCSS editor.

These PNGs are local-only (gitignored). They can include whatever is on the live Gelbooru tab, including post thumbnails.

```bash
npm run preview:install
```

Then reload the Cursor window and recapture a page.
